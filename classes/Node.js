export class Node {
    /**
     * Creates an instance of Node.
     * @param {String} name nome do nó
     * @param {Number} value valor do nó
     * @memberof Node
     */
    constructor(name, value) {
        this.name = name;
        this.value = value;
    }
}
